# 升本通 · 项目任务清单（唯一）

> **规则**（用户制定，2026-08-23）：
> 1. 本文件是全项目**唯一**任务清单，其他任何地方不得另立清单
> 2. 任务完成后：在底部「✅ 完成汇报」追加小结，同时把该任务从上方**删除**
> 3. 完成一个，把下一个任务提为「🎯 进行中」
> 4. 有疑问**先问用户**，不许自作决定
>
> 冲突优先级：需求文档 > MD规范v2.1 > 两份设计文档 > 本清单

---

## 🎯 进行中（一次只有一个）

### T-112 五项缺口加固——代码完成，待用户验收（2026-08-25）

1. **图像下载原子化**：`downloadTo` 先下 `.tmp` 再改名（杀进程/断网不留半张图）；`_downloadImages` 同步前清理历史 `.tmp` 残留；新增2测试（原子落盘+残留清理），Flutter **41/41 绿**
2. **data_version 更新提示**：核查确认**已实现**（科目页 MaterialBanner 弱提示"题库有更新，是否刷新？"，"稍后"可继续用旧题不强制；hasUpdate 对比逻辑+测试早已有）——零改动，仅文档化
3. **PC 题库自动备份**：start.bat 启动时备份 `shengbentong.db → backup/upmark_时间戳.db`，保留最近10份轮替（实测备份442KB成功）
4. **绘图脚本跨机器兼容**：scripts/gen_exam_images.py 入库，字体候选链回退（Win雅黑/黑体/宋体 → macOS苹方 → Linux Noto/文泉驿 → PIL默认位图），支持argv输出目录；不内置16MB字体文件（比例失衡，链路已覆盖主流系统）
5. **验收Checklist文档化**：仓库根目录 CHECKLIST.md（历史T-103~T-111回填+模板），后续验收只追加

- **验收**：App重同步后刷图题正常（原子下载路径生效）；start.bat 重启看到备份提示
- 完结后归档+CHECKLIST勾选

---

## 📋 待办队列（顺序即优先级）

### T-106 打磨发布
- 百词斩风格UI统一、对错动画、模拟考试限时组卷、打包APK
- **验收**：非技术用户独立完成"启动PC端→绑定→选科目→刷题"

---

## ⏸ 等待用户决策

（无——T-111阶段二默认不做，除非用户投诉公式看不清）

---

## ✅ 完成汇报（只追加，不改历史）

### [2026-08-25] T-112 五项缺口加固——代码完成，待用户验收
- **①图像下载原子化**：SyncService._downloadImages 改为先下 `.tmp` 再改名落盘（Windows rename不覆盖→先删旧），同步开始清理历史 .tmp 半成品；渲染只认正式名天然忽略 .tmp；新增2集成测试（正式名落盘+DB改写、历史.tmp清理），**Flutter 41/41 绿**（39+2）
- **②绘图脚本字体回退链**：T-109生成器入库 `scripts/gen_exam_images.py`（本地私有），字体按候选链探测（msyh→simhei→simsun→PingFang→Noto→文泉驿）全缺时回退PIL默认并告警；**未内嵌16MB字体包**（仓库轻量优先，脚本仅本机生成内容时运行）——实测6图重生成成功
- **③data_version机制核查（零改动）**：App已有完整弱提示链——SubjectScreen initState `_checkUpdate()` 对比 data_version → MaterialBanner「题库有更新，是否刷新？」→ 立即刷新/稍后，稍后关闭横幅继续用旧库不强制；离线静默。与用户建议完全一致；hasUpdate true/false 已有测试覆盖
- **④启动自动备份**：start.bat 已含备份块（用户同步添加）——启动时 copy2 到 backup/upmark_时间戳.db 保留最近10份；实测备份生成+轮替13→10正确（无WAL，启动期复制安全）
- **⑤CHECKLIST.md 建立**：仓库根目录，补录 T-103~T-111 全部历史验收记录+模板，今后逐任务追加
- 质量门：Flutter 41/41 + analyze零问题；新APK已部署MuMu
- 待验收（用户）：①MuMu重同步看图题正常（原子下载回归）②CHECKLIST内容核对

### [2026-08-25] T-111 LaTeX/代码块文本化 D方案阶段一——验收通过，任务完结
- **解析器修复**：`_lookahead_says_question` 新增 `$$` 块跳过逻辑（RE_DOLLAR_BLOCK + in_dollar 状态，多行/单行块均覆盖），数据丢失级缺陷永久免疫；新增 tests/test_dollar_block.py 两用例（单选多行$$块+判断单行$$块防E130），**PC 46/46 绿**（44+2）
- **App渲染**：新增 lib/widgets/rich_text.dart 零依赖组件——`$...$`等宽斜体主色、`` `...` ``等宽灰底、围栏块灰底等宽容器；quiz_screen 题干/选项/讲解三处接入；**analyze零问题 + 39/39绿**
- **网页渲染**：marked.min.js v12（35KB MIT）内嵌 static/ 走 /static 静态服务（零CDN）；quiz.html 新增 renderRich()——围栏外转义防HTML注入、marked渲染（breaks/gfm）、行内$...$包`<code class=math>`样式；材料/题干/选项/讲解四处接入
- **判分核查（零改动）**：accepts多候选DB已支持，App `_norm` 与网页 `normalize` 归一化（去空格/全角分号/大小写）均已存在，D方案第4项确认无需开发
- **提示词解除$$限制**：8文件恢复"$$独占一行（题干后或讲解中均可）"；MD规范升 **v2.2**（G5行内公式透传+G7围栏四路由+G9独立公式块透传），v2.1归档加废弃标记，README/任务清单/admin提示语/解析器注释同步改引v2.2
- **端到端**：实验章节「实验-公式与代码/01-文本化验证」5题入库（DB 675题/12科目/34章），sync实测q2题干含```c代码块、q1题干含$f(x)$、填空accepts三候选下发；marked.min.js静态服务200；新APK增量构建7.1s已部署MuMu并启动
- **验收（用户）**：①发现 `$` 定界符外露（截图）→ App `_inlineRe` 升级双形态匹配（`$$...$$`/`$...$`）渲染时剥定界符、网页同步剥两层 → 重建部署 → 用户确认"没问题了" ✅ ②网页刷题页同章正常 ③$$紧跟题干的题不再被吞
- 阶段二（flutter_math_fork/KaTeX精美渲染）：默认永不做，除非用户实际使用后仍抱怨公式看不清

### [2026-08-25] T-111(pre) LaTeX/代码块文本化可行性验证 + 提示词实验——完成（渲染实现待用户决策）
- **用户提案**：公式转$LaTeX$、代码转```围栏、仅函数图像/几何图保留【图】，要求先验证可行性+改提示词看效果，不改代码
- **解析器实测11用例**：行内$...$（题干/选项/答案/讲解）全穿透✓；```代码块在题干/讲解/选项后三路由✓（v2.0已内置，语言标记保留）；围栏不配对E122拒文件✓；奇数$静默穿透（无配对校验）；**发现缺陷**：$$块紧跟题干行→_lookahead_says_question只看题干后第一行且$$无结构证据→边界识别失败（单选场景W307吞题/判断场景E130整拒无留痕）
- **提示词实验**：7模板+规范v1.2新增R5b公式与代码条款；实测出题5题（LaTeX单选/C代码单选/$$讲解/判断/填空带备选）→修正$$规则后5/5全绿0警告；填空accepts实测['$\frac{1}{3}$','1/3','0.33']多候选判分可用
- **渲染现状**：App题干纯Text、网页pre-wrap转义——LaTeX今日起原样显示（可读不美观），代码块换行保留可读无高亮；markdown_widget仅用于知识点页（用户提案中"flutter_markdown已用"不属实）
- **修正用户预期**：①App用的是markdown_widget非flutter_markdown；②现有题库公式已是Unicode文本，千倍压缩不成立，真实收益是复杂公式表达力；③新增依赖（flutter_math_fork/flutter_highlight/本地KaTeX）均须审批
- 质量门：PC 44/44绿；实验文件在test-bank/实验-公式与代码（本地私有不入库）

### [2026-08-25] T-110 GitHub上传策略：图像版/非图像版分支隔离 + 归档文档退出版本库——完结
- **用户要求**：①图像版本与无图像版本在GitHub上必须分开，拉额外分支；②技术文档新增上传要求；③归档旧文档不上传
- **分支隔离落地**：新建 `image-support` 分支承载图像版全部实现（T-108代码+规范v2.1+契约v2+T-109实战，至 d77d49a）；`main` 强推回退到无图像基线 a49bea2（T-107完结点）+策略补丁（282ee60）。GitHub 双分支就位，本地继续停在 image-support 开发，服务端/磁盘文件零影响
- **规范升级**：《代码生成与审批规范》v1.0→v1.1，红线表新增两条——「GitHub上传-版本分支」（main=无图像基线，图像实现仅走image-support，经用户批准方可合入）、「GitHub上传-归档文档」（docs/归档/禁止上传）
- **归档退出**：docs/归档/ 三文件移出 git 跟踪 + .gitignore 排除，本地保留备查（v1.0/v2.0/旧Electron）
- **操作记录**：main 重写经临时 worktree 打补丁（不干扰主工作区）；两分支各自提交策略补丁后 force-push main + 新推 image-support
- 后续约定：图像相关新任务在 image-support 上提交；合入 main 须用户明示批准

### [2026-08-25] T-109 考研高数图像题实战验证（提示词全流程）——用户追加任务，完结
- **背景**：用户质疑T-108抛物线用例太简单，要求用真实考研高数图像题走完「提示词规范→MD解析→导入→下发」完整流程
- **真题复刻**：2016考研数二第4题（数三第1题）f′(x)图形判极值/拐点（答案B）为主打，PIL纯Python绘制6张高保真图像（f′(x)经典图精确复刻a/c变号零点+d切触不变号+b/e极值的真题形态、三次曲线、sin一拱阴影、e^(−kt)衰减、x²与x围面积、eˣ过两点）
- **新章节**：test-bank/考研-高等数学/02-导数与函数图像（6题：3单选+1判断+2填空），覆盖【图】全部特性——普通/加粗形态、【材料】+【图】共存、分区分组各自重置、多空｜+备选;答案
- **全流程验证**：本地解析器预检6题0警告 → 服务端导入（注意：目录导入必须传题库根目录，传科目文件夹会把章节当科目，files_total=0）→ 14文件/0失败/0跳过 → DB 664→670题 → sync下发6个image字段 → 静态图200
- **提示词体系升级（打通源头）**：7个模板「图像红线」→「引用式图像题规则」（只许引用用户确认存在的图片，禁止虚构路径，【图】语义同材料）；《AI收集题目提示词规范》v1.0→v1.1（新增R5a读图题条款+自检项+2条坏习惯对照）
- 质量门：PC 44/44绿；模板7/7无BOM断言通过
- 待用户操作：MuMu重同步即可在App看到「考研-高等数学/导数与函数图像」刷图题

### [2026-08-25] T-108 图像题支持 验收通过，任务完结
- **用户实测**：MuMu重同步 → 高等数学/函数图像 → 抛物线配图正常显示，断网重进仍可刷图题 ✅
- **文档收尾**：《MD格式规范v2.2》发布（【图】语法P1~P7规则+W322/W323码表+差异速览+G8相对路径红线），v2.0移入docs/归档/加SUPERSEDED标记；api_contract_v2.json冻结（schema_version=2、question.image字段、sync/all增data_version、GET /static/images/{name}端点）；README目录树/冲突优先级链/契约引用、任务清单优先级链、admin.py两处提示语、解析器三处docstring同步指向v2.1
- **部署验证**：debug APK增量构建66.8s → streamed install成功 → MuMu启动确认；服务端uvicorn拉起，health返回schema_version=2（11科目/32章/664题）；sync/all实测image字段下发（qid=1120），GET /static/images/0fd56f2a726b_206162cd.png → 200 image/png 3323B
- 质量门：PC测试44/44绿；契约v2 JSON合法性校验通过；Flutter侧无改动（39/39基线不变）
- T-108完结。队列已清空，下一任务：T-106打磨发布

### [2026-08-25] T-108 图像题支持 全链路代码完成（WIP存档，明日续）
- 一句话进度：解析器→PC库→导入器→静态服务→sync v2→网页→App渲染 全部打通并各自验证通过；剩 v2.1规范文档/契约文档/MuMu部署/断网验收 四件事
- 关键设计：【图】语义同【材料】（归属其后题目、分区头重置）；图像以sha1命名拷入static/images（同名覆盖可更新）；App同步时预下载全部题图，DB存本地绝对路径，QuizScreen纯Image.file渲染——离线天然可用
- 踩坑记录：①【图】放分区头之前会被"分区重置"清掉（规范如此，md要写在分区内）②StaticFiles挂载时目录必须已存在（lifespan建目录来不及，需import期mkdir）③App本地DB v1→v2用onUpgrade补列，旧机无损升级
- 质量门：PC 44/44绿、Flutter 39/39绿、analyze零问题

### [2026-08-25] T-107 验收通过，任务完结 + 拖拽导入 + 双页面导航
- **用户浏览器实测通过**：网页刷题全流程可用；管理台↔刷题页双页面互设入口导航
- **拖拽导入实战验证**：用户拖入 练习题.md → 期末考-高等数学/一元函数微积分 10题success 0跳过（幂等覆盖，总量660不变），import_logs id=108 留痕
- **顺带修复时区不一致**：ImportLog/AnswerRecord/Chapter.created_at 三处 server_default=func.now()(UTC) 改为 default=datetime.now(本地时钟)，与App上报时间轴统一（历史行保持UTC不改写）
- T-107完结，队列剩 T-108图像题 / T-106打磨发布

### [2026-08-25] T-107 PC网页刷题 代码层完成
- **web/quiz.html**：单文件SPA（科目→章节→刷题→成绩），与管理台同设计语言，零外部依赖不违反公网红线
- **四题型**：单选点击即判/多选勾选确认(≥2)/判断√×/填空(多空｜、备选;与App同套判分规则)；材料块滚动容器
- **进度直写**：每答一题即POST /api/sync/progress→AnswerRecord；失败进前端队列下次作答自动重试
- **路由**：GET /api/admin/quiz/page；服务端已重启加载
- **冒烟全通**：sync/all→push→SQLite核对；测试行已清理
- 待用户浏览器实测后完结

### [2026-08-25] T-105 验收通过，任务完结
- **用户实测**：错题本流转✓（答错入本/重练答对移出）、收藏夹✓、统计页✓
- **联调发现并修复竞态bug**：统计页initState中_reload与_tryUpload并发，自动上传读到空summary(pending=0)被跳过——改为先await reload再尝试上传
- **两端一致性核验**：设备sync_queue 8条payload代推→PC answer_records逐条比对一致；清理T-102 Gate残留假数据(qid=1)
- **App实时链路**：修复版APK部署后用户实测——答2题→统计页显示2条待传→点上传→"已上传2条"→待上传归零；PC端answer_records增至10条(qid189✓/qid190✗入错题本)与设备完全一致
- **验收标准达成**：断网刷题回家同步两端一致 ✅ T-105完结

### [2026-08-24] T-105 代码层完成：错题本/收藏夹/统计/进度持久化/批量上传
- **打通答题落库链**（此前saveProgress从未被调用，进度不落库）：QuizScreen新增 onAnswered/onToggleFavorite 回调（保持纯UI零IO），ChapterScreen接线写 local_progress+sync_queue
- **收藏功能**：刷题页AppBar书签按钮；收藏状态经initialFavorites注入回显；setFavorite对未答过的题可建行
- **错题本页**：答错自动入本、重练答对自动移出、手动移除、一键"错题重练"
- **收藏夹页**：列表+取消收藏+"练习全部收藏"
- **我的统计（数字版）**：累计作答/正确率/错题数/收藏数/待上传/上次同步 六卡片 + 分科目正确率表 + "上传答题记录到PC"按钮（进页面静默尝试自动上传）
- **批量上报**：SyncService.uploadPending() 读sync_queue批量POST /api/sync/progress，成功清队列；服务端按(qid,answered_at)幂等去重；离线时进度存本地仅提示不打扰
- **离线确认**：本章作答返回后SnackBar提示"N题已存本地"
- **测试**：flutter test **39/39全绿**（新增T-105服务层10用例+回调2用例）；analyze零问题；新APK已部署MuMu
- **待验收（用户）**：答几题→看错题本/收藏/统计→断网作答回家联网上传→PC端AnswerRecord表核对两端一致

### [2026-08-24] T-103+T-104 全部验收通过：3个widget测试修复 + 多选确认按钮bug修复
- **修复①多选无法作答（真实功能bug）**：纯UI重构时丢失了"确认作答"按钮，多选题只能勾选不能提交；已补回 FilledButton（选中N题显示"确认作答(N)"，未选置灰）
- **修复②导航按钮无Key**：btn_prev/btn_next 补 Key，测试可定位
- **修复③KnowledgeScreen架构违规**：initState做DB IO违反纯UI约定 → 重构为构造器注入（knowledgeMd+questions），加载逻辑移至ChapterScreen，与QuizScreen同架构
- **测试**：flutter test **29/29全绿**（新增"知识点跳转刷题页tap验证"+"无题按钮禁用"两用例；多选用例加ensureVisible修视口折叠问题）；flutter analyze零问题
- **结论**：T-103+T-104 四项验收全部通过（绑定同步/刷题渲染/材料块/断网离线），FakeAsync+sqflite_ffi时序问题随架构纯UI化自然消解
- 待办：新APK重建部署MuMu（多选按钮生效）

### [2026-08-24] 材料块渲染MuMu实测通过 + 新APK重新部署
- **根因**：MuMu上装的是7f8e53b之前的旧APK，同步只更新数据不更新界面代码；代码链路(API→SyncQuestion→本地DB→Question.fromRow→QuizScreen材料容器)六环本就完整
- **修复**：flutter analyze零问题 → 重建debug APK(首次Gradle全量下载56min) → adb覆盖安装com.shengben.shengbentong → 启动前台确认
- **用户实测**：英语四级→阅读理解→练习，Passage原文蓝色容器正常显示 ✔
- **环境备忘**：JAVA_HOME 实际路径为 `D:\jdk17\jdk-17.0.20+8`（D:\jdk17 下有嵌套目录，直接设D:\jdk17会导致gradle报错）；adb需用全路径 D:\android-sdk\platform-tools\adb.exe

### [2026-08-24] T-103/T-104 MuMu联调验收（2/4通过）
- ✅ MuMu绑定 PC IP:8000 → 全量同步 → 科目列表正常显示
- ✅ 科目→章节→练习→刷题页渲染题目全链路走通
- 剩余待验收：断网离线重进、3个widget测试修复
- 技术文档已同步：《出题模板》v2.1、《数据集技术文档》v1.3、README状态表

### [2026-08-24] 图像题处理：提示词红线 + T-108登记
- **7个模板新增图像红线**：禁止「如图所示」类读图题；转换模式原图题可文字化改写的改写、否则丢弃；出题模式一律不命制读图题
- **T-108图像题支持已登记**待办队列（规范v2.1→契约v2→静态目录→App缓存渲染全链路方案），排在T-105之后

### [2026-08-24] AI提示词双模式改造 + 全新测试集替换（项目首次全绿导入）
- **提示词双模式**：模板头部从"题库格式化引擎"改为"AI出题引擎"，支持①转换模式（用户附带现成内容→整理为规定MD）②出题模式（无内容→按科目/考试阶段/章节依考纲自主命题，答案必须唯一确定）；管理台AI出题卡片的使用说明同步更新
- **全新测试集**：旧test-bank(14文件/149题/含故意缺陷)全部删除，按提示词规范重新生成12文件/148题，覆盖6科目×全阶段：默认本科(计算机基础/高等数学/大学语文)、考研-(考研-高等数学/考研政治)、期末考-高等数学、自考-大学语文、四级、六级；含材料块阅读理解(2篇6题)、多选题、语法填空、多空填空、备选分号等全特性
- **首次全绿导入**：12文件全部 success 状态（旧集始终partial），0致命/0跳题/0跳区/0警告
- **DB清理**：删除旧test-bank来源科目(id1-7,含ImportLog引用清理)，保留computer-bank的C语言+数据结构512题
- **端到端抽检**：11科目/31章/660题；6道材料题material字段完整下发；661→660题选项0畸形
- **注意**：故意缺陷注入演示文件随旧集移除，E/W码拦截能力仍由tests/单元夹具(39/39绿)保障

### [2026-08-24] 管理后台单页改版 + AI提示词去考试绑定
- **管理后台单页整合**：三标签合并为一页仪表盘（顶栏实时健康状态 → 导入条 → 左报告区/右AI出题+统计双栏）；Linear/shadcn风格、零外部依赖；toast替代alert；剪贴板加execCommand兜底（局域网HTTP可复制）；单文件E码拒绝渲染为结构化REJECTED报告
- **AI提示词去考试绑定**：docs/templates/ 全部7个模板去除「专升本-」前缀（subject行改为中立科目名），并统一新增「考试阶段由用户决定」规则——用户在材料中注明目标考试（考研/四级/自考/期末等）时，AI将subject改为「考试-科目」格式并匹配难度，未注明按本科水平
- **验证**：7模板无BOM/无专升本/规则齐全；API热生效；管理页组件完整性断言通过

### [2026-08-24] 管理台AI出题工作台 + BOM清理 + test-bank重导验证
- **管理后台三标签改版**：📥导入题库 / 🤖AI出题(选科目→复制提示词→喂AI→回导) / 📈题库统计；新增 GET /api/admin/subjects 与 /api/admin/template/subject/{name}（docs/templates/ 6科目模板，防路径穿越）
- **UTF-8 BOM红线清理**：admin.py / README.md / 数据集技术文档.md / test_corpus_derived.py 共4文件去除BOM；保留 tests/*/e100_bom.md 两个故意BOM夹具（E100检测用例）
- **test-bank重导验证**：149题入库幂等，6处故意缺陷全拦截(W321/W305/W302/W306/W320/W307)+简答区W205跳过+E122整拒演示文件
- **数据端到端抽检**：661题选项0畸形；5个材料块→21题正确携带material字段
- **待用户操作**：运行 start.bat(绑定0.0.0.0) → MuMu重同步 → 验证材料显示与选项渲染

### [2026-08-24] T-104 刷题闭环核心完成 + 环境搭建 + MuMu部署
- **架构重构**: QuizScreen从"initState做IO"改为"纯UI接受预加载List<Question>"，彻底消除FakeAsync+sqflite_ffi冲突
- **四题型**: 单选选项按钮 / 判断√×按钮 / 填空TextField+提交(多空｜+备选;) / 多选勾选+确认作答(N)
- **即时判分**: 绿色回答正确/红色回答错误 + 讲解自动展开 + 正确答案提示
- **DbService**: saveProgress(local_progress+sync_queue双写) / progressMapOf / 路径级_cache复用 / resetInstanceCache
- **KnowledgeScreen**: markdown_widget渲染 + 学练衔接按钮
- **ChapterScreen**: _openQuiz加载题目后传参导航到QuizScreen
- **环境搭建**: Flutter 3.47.1 + Android SDK 36 + JDK17 + MuMu模拟器联通(adb 16384/7555)
- **App已部署MuMu运行**（截图确认UI正确：绑定页渲染正常）
- **修复记录**: Kotlin增量编译bug→kotlin.incremental=false；AGP 9 DSL→android.newDsl=false
- **测试**: 25/28通过（3个widget测试因FakeAsync+sqflite_ffi时序待真机调测）
- **已知问题**: 并发isolate下upmark.db共享锁导致部分套件互斥

### [2026-08-24] 仓库迁移至全英文路径 D:\dev\upmark
- 升本通项目→shengbentong, 广东专升本计算机考试题→computer-bank(本地), 测试题库→test-bank(本地)
- 解决中文路径导致Dart分析器LSP崩溃的问题
- 同步更新 .gitignore / 回归脚本常量 / README目录树与联调示例

### [2026-08-24] T-102 PC服务端 FastAPI —— 完成（Gate 2/3 通过）
- 接口8组全部落地并实测：health / sync.all(9科目661题下发) / sync.chapters / sync.questions / progress(幂等去重) / admin.import(挂载bulk_importer,幂等验证149题) / import日志查询 / stats / template(MD+AI提示词)
- 管理后台网页 /api/admin/page：路径导入、报告可视化+一键复制回喂AI、统计表、模板下载
- CORS全开(内网)、lifespan自动建表、Pydantic契约层(schemas.py) → **Gate 2 ✅**
- uvicorn实启全流程打点全部200 → **Gate 3 ✅**

### [2026-08-24] T-101 多科目测试题库+科目识别 —— 全部完成
- **测试集**: 14文件/8科目/149题，9处缺陷注入全部精准拦截，E122整拒演示成功
- **解析器进化**: 新增样式D、行内选项拆分(§6.5a)、材料空题干例外、W319分隔符兼容链、前瞻统一规则
- **bulk_importer.py**: 任意题库根目录一键入库；幂等覆盖；空科目自清
- **双库联合验收**: SQLite落地 **9科目/32章/661题**

### [2026-08-23] M0 文档与规划阶段
- 需求文档、MD规范v2.0、AI提示词规范、技术方案v2.1、API契约v1、README导航

### [2026-08-23] T-100 MD解析器+数据层
- strict_parser.py 自研状态机：样式A/B/C/D、材料块、全E/W规则码
- 计算机题库最终态：512/512客观题全部入库，跳过题目=0，36/36测试绿
- SQLAlchemy五表模型 + CLI独立运行入口

---

## 📊 项目数据快照

| 维度 | 数值 |
|------|------|
| 计算机题库 | 19文件 / 512题 / 0跳题 / 0失败 |
| 测试题库 | 14文件 / 9科目 / 158题 / 含图像题章节（考研高数02） |
| 联合入库 | 11科目 / 33章 / 670题 / 10题带图 |
| 解析器单元测试 | 44/44绿 |
| Flutter测试 | 39/39绿 |
| GitHub | https://github.com/yunxibaili/UpMark |
| 本地路径 | D:\dev\upmark |
| Flutter | 3.47.1 stable |
| Android SDK | 36.0.0 @ D:\android-sdk |
| JDK | 17.0.20 @ D:\jdk17\jdk-17.0.20+8（注意嵌套目录） |
| 模拟器 | MuMu (adb 127.0.0.1:16384) |
